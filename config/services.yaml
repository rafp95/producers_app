# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        bind:
            $projectDir: '%kernel.project_dir%'
            $baseUrl: '%env(EXTERNAL_API_BASE_URL)%'
            $username: '%env(EXTERNAL_API_USERNAME)%'
            $password: '%env(EXTERNAL_API_PASSWORD)%'

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    App\ApiResource\OpenApiFactory:
        decorates: 'api_platform.openapi.factory'
        arguments: ['@.inner']
        autoconfigure: false

    ApiPlatform\Symfony\Action\DocumentationAction: '@api_platform.action.documentation'

    App\Domain\Repository\ProducerRepositoryInterface:
        class: App\Infrastructure\Repository\InMemoryProducerRepository

    App\Infrastructure\EventListener\ExceptionListener:
        tags:
            - { name: kernel.event_listener, event: kernel.exception, priority: 10 }

    App\Infrastructure\EventListener\TimestampableListener:
        tags:
            - { name: doctrine.event_listener, event: prePersist }
            - { name: doctrine.event_listener, event: preUpdate }

